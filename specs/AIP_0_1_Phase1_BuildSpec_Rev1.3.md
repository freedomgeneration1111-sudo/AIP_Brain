# AIP 0.1 Phase 1 BuildSpec — Rev 1.3

**SINGLE SOURCE OF TRUTH** for this project.

This markdown is a text extraction from the authoritative .docx. The .docx is the official reference.

---

AIP 0.1 Phase 1 BuildSpec — Rev 1.2Product: AI Poiesis (AIP) version 0.1Architecture Revision: 5.2Build Phase: 1 — Workflow 0.1 Node ImplementationSpec Revision: 1.3 (protocol completeness — 3 final amendments)Date: May 2026Status: Build Specification — for Grok Build executionSupersedes: Phase 1 BuildSpec Rev 1.2DEFINER: Moses JorgensenRevision Log1.0 → 1.1DeltaIssueFixD1VectorStore Protocol mismatch with Phase 0 ANNEX DReplaced RetrievalHit with unified Chunk type; aligned VectorStore interface to upsert/retrieve/delete with list[float] query_vector and list[Chunk] returnD2Phase 0 assumption table referenced non-existent CHUNK-0.6/0.7/0.8Corrected to match actual Phase 0 deliverables; config/aip.config.toml everywhereD3foundation/schemas.py modified without explicit dependencyListed as append-only file in CHUNK-1.0a FILES with explicit instruction not to modify existing Phase 0 enumsD4retrieve() string query path contradicts architecture §1.2Added deterministic fake_embed() in CHUNK-1.1 tests; documented Phase 1 embedding limitation in proseD5RerankWeights and confidence_threshold hardcoded, violating §1.8 toggleable requirementMoved defaults to config/aip.config.toml under [retrieval]; loaded via config loader; gate asserts config-sourced valuesR1CHUNK-1.0 at 8k tokens risky for Grok BuildSplit into 1.0a (schema + init, L1, ~3k tokens) and 1.0b (implementation, L2, ~5k tokens)R2No TraceStore logging in CHUNK-1.1 and 1.2Added trace_store: TraceStore parameter to retrieve_for_synthesis and validation failure logging in structural_validate caller contractR3ECS transition not recorded in event_logAdded event_log assertion in CHUNK-1.6 testsR4No token budget annotation on ANNEX codeAdded ESTIMATED_TOKENS to every ANNEX block1.1 → 1.2FixIssueChangeF1VectorStore.store() raises NotImplementedError, will fail Phase 0 test_storage_contracts.pyImplement store() as thin upsert wrapper with zero-vector for Phase 0 backward compatF2retrieve_for_synthesis logs failure_type="B" (Procedural Gap) but low confidence is Type A (Missing Context) per Appendix EChanged failure_type from "B" to "A" in both trace_store.write_event callsF3config: dict | None signature conflicts with Phase 0 AipConfig (Pydantic) return typeAccept AipConfig | dict with model_dump() fallback in implementationF4Prose says “no hardcoded defaults” but from_config uses .get(..., 0.60) — spec/code mismatchUpdated prose to acknowledge defaults match §8.3 but are overridden by config — satisfies §1.8 toggleable requirementF5load_extension("vss0") double-call will fail twice on systems without vss0Second attempt tries "./vss0.so" as common local path fallbackR1’test_schema_additions uses dir(VectorStore) which passes even if methods are missing on ProtocolChanged to assert hasattr(VectorStore, "upsert") etc. — simpler and deterministicR2’TraceStore method name not verified against Phase 0 ProtocolConfirmed Phase 0 uses write_event; added clarifying comment in CHUNK-1.0a proseR3’CHUNK-1.0a amends protocols tested by CHUNK-0.6 but doesn’t declare dependencyAdded CHUNK-0.6 to CHUNK-1.0a DEPENDS-ONR4’No ESTIMATED_TOKENS annotation on CHUNK-1.1 test fileAdded <!-- ESTIMATED_TOKENS: ~1,200 --> for test file1.2 → 1.3FixIssueChangeP1Phase 0 left TraceStore, EventStore, ArtifactStore as ... stubs — Grok will fail without method signaturesAppended write_event / write / read method signatures to protocols.py in same amend-by-addition block as VectorStoreP2ecs_store.transition call missing actor and reason params required by Phase 0 contract and §1.7 sovereigntyUpdated CHUNK-1.6 commit.py to pass actor="definer_gate" and reason="DEFINER approved"; updated FakeEcsStore in test to accept new paramsP3field used in schemas.py append but from dataclasses import field not shown — Grok will get NameErrorAdded field to dataclasses import line in schemas.py ANNEX blockPhase 1 ScopePhase 1 implements the six node functions of Workflow 0.1 (Appendix F) as standalone, testable functions. The YAML engine that composes them into a workflow graph is deferred to Phase 2. Phase 1 also delivers the sqlite_vss VectorStore adapter required by the retrieve node, and a network-isolation / no-hardcoded-model-names gate that covers all Phase 1 code.In scope:CHUNK-1.0a: Schema additions + adapter package init (L1, append-only to Phase 0 file)CHUNK-1.0b: sqlite_vss VectorStore adapter (L2, adapter layer)CHUNK-1.1: retrieve_for_synthesis — L2 retrieval + low-confidence gate + four-factor reranking (config-loaded)CHUNK-1.2: structural_validate — L3a Stage 1 deterministic validation (zero tokens)CHUNK-1.3: Synthesis node stub — agent node interface (no model call in CI)CHUNK-1.4: Adversarial eval stub — L3b interface (no model call in CI)CHUNK-1.5: DEFINER gate stub — dialog node interfaceCHUNK-1.6: Commit stub — artifact write + ECS transition + event_log recordCHUNK-1.7: Network isolation and model-name gate — cross-cutting testOut of scope:YAML workflow engine (Phase 2)pgvector adapter (Phase 4)Actual model API calls (stubs return deterministic fixtures in CI)Real embedding pipeline (Phase 1 uses fake_embed(); real embedding slot wired in Phase 3)Beast, Vigil actorsUI / MCP / CLI surfacesL4 trajectory regulation (Phase 3)Phase 0 Assumptions (Corrected)Phase 1 chunks depend on the following Phase 0 deliverables being merged and green:CHUNK-0.xDeliverablePhase 1 Dependency0.1Directory layout, pyproject.toml, uv config, src/ structureAll chunks0.2Config system — config/aip.config.toml, foundation/config.py (slot resolution, config loader)1.0b, 1.1, 1.30.3foundation/schemas.py — ContractRule, ContractTier, EcsState, FailureType enums1.0a appends, 1.2, 1.5, 1.60.4foundation/protocols.py — all Protocol ABCs (VectorStore, TraceStore, ArtifactStore, EcsStore, etc.)1.0a amends, 1.0b, 1.1, 1.2, 1.60.5Database init — db/trace.db with trace_events table, db/state.db, db/events.db1.2, 1.60.6Green gates: test_storage_contracts.py + test_layering.pyAll L2 chunks0.7Green gates: test_trace_schema.py + database schema validation1.2, 1.60.8Governance tests: test_no_hardcoded_models.py, test_no_network.py + CLI stub1.7 extendsCritical note on CHUNK-1.0a: This chunk appends to foundation/schemas.py and amends foundation/protocols.py. It is the only Phase 1 chunk that modifies a Phase 0 file, and it does so by append only (schemas) and amend by addition (protocols — adding methods to the existing VectorStore Protocol, and adding method signatures to the stub TraceStore, EventStore, and ArtifactStore Protocols). No existing Phase 0 code is deleted or rewritten.If any Phase 0 chunk is not merged, the depending Phase 1 chunk cannot start.Dependency DAG (Revised)CHUNK-0.1 ─┬─ CHUNK-0.2 ─┬─ CHUNK-0.4 ─┬─ CHUNK-1.0a ─── CHUNK-1.0b ─── CHUNK-1.1 ─── CHUNK-1.3 ─┬─ CHUNK-1.5 ─── CHUNK-1.6            │              │              │                                                 │            ├─ CHUNK-0.3 ──┤              ├─ CHUNK-1.2 ────────────────────────────────────────────┤            │              │              │                                                 │            └─ CHUNK-0.5 ──┴─ CHUNK-0.6 ─┴─ CHUNK-1.4 ────────────────────────────────────────────┘            │              │                                                                │            └─ CHUNK-0.7 ──┴─ CHUNK-0.8 ─────────────────────────────────────────────────── CHUNK-1.7Linearized build order:  1.0a → 1.0b → 1.1 → 1.2 (parallel with 1.1) → 1.3 → 1.4 (parallel with 1.3) → 1.5 → 1.6 → 1.7Parallel groups:  Group A: [1.0a]                                    — schema + protocol additions  Group B: [1.0b] (after 1.0a)                       — sqlite_vss implementation  Group C: [1.1, 1.2] (after 1.0b)                   — retrieval + validation  Group D: [1.3, 1.4] (after 1.1, 1.2)               — synthesis + eval stubs  Group E: [1.5] (after 1.3, 1.4)                     — definer gate  Group F: [1.6] (after 1.5)                          — commit  Group G: [1.7] (after all)                          — cross-cutting gateCHUNK-1.0a: Schema Additions + Adapter Package InitCHUNK-1.0a: Schema Additions + Adapter Package InitPHASE: 1DEPENDS-ON: CHUNK-0.3, CHUNK-0.4, CHUNK-0.6CODER-PROFILE: L1CONTEXT-BUDGET: ~3,000 tokensFILES:  foundation/schemas.py (append only — do not modify existing Phase 0 enums)  foundation/protocols.py (amend by addition — add VectorStore methods + TraceStore/EventStore/ArtifactStore method signatures)  adapter/__init__.py  adapter/vector/__init__.pyINTERFACES:  @dataclass  class Chunk:      id: str      content: str | None      score: float      metadata: dict      domain: str | None  # Protocol amendments in foundation/protocols.py (amend by addition):  class VectorStore(Protocol):      async def upsert(self, id: str, embedding: list[float], content: str, metadata: dict, domain: str | None = None) -> None: ...      async def retrieve(self, query_vector: list[float], domain: str | None = None, top_k: int = 10) -> list[Chunk]: ...      async def delete(self, id: str) -> None: ...  class TraceStore(Protocol):      async def write_event(self, session_id: str, node_type: str, failure_type: str, outcome: str, detail: str | None = None) -> None: ...  class EventStore(Protocol):      async def write_event(self, event_type: str, actor: str, artifact_id: str, from_state: str | None = None, to_state: str | None = None, **kwargs) -> None: ...  class ArtifactStore(Protocol):      async def write(self, id: str, content: str, metadata: dict) -> None: ...      async def read(self, id: str) -> str: ...TESTS:  tests/test_schema_additions.pyGATE: uv run pytest tests/test_schema_additions.py -xvsProseThis chunk establishes the shared data types and protocol method signatures that all subsequent Phase 1 chunks depend on. It does four things:1. Append Chunk and RetrievalResult dataclasses to foundation/schemas.py. The Chunk dataclass replaces the original RetrievalHit concept. There is one retrieval type, not two. The Chunk fields (id, content, score, metadata, domain) match the unified interface that both the sqlite_vss adapter (CHUNK-1.0b) and the retrieve_for_synthesis function (CHUNK-1.1) consume and produce. The RetrievalResult dataclass wraps the low-confidence gate result with status, hits, max_confidence, and message. Append only — do not modify or reorder the existing ContractRule, ContractTier, EcsState, FailureType definitions from Phase 0.2. Amend VectorStore Protocol in foundation/protocols.py. Phase 0 ANNEX D defined VectorStore with store(chunk) and retrieve(query, domain, limit). That interface is too narrow for vector work: it lacks upsert semantics, embedding vector pass-through, and delete. The amended Protocol adds upsert, retrieve (with query_vector: list[float]), and delete. This is an addition to the existing Protocol, not a replacement — if Phase 0 test code references the old methods, those must still pass. The amendment is additive: the old store() method is deprecated but retained for backward compat. CHUNK-1.0b implements store() as a thin upsert wrapper with zero-vector so Phase 0 test_storage_contracts.py continues to pass. Note: Phase 0 TraceStore Protocol defines write_event (not log or append) — this is the method name used in CHUNK-1.1 and must not be changed.3. Create adapter/__init__.py and adapter/vector/__init__.py as empty package files so that CHUNK-1.0b has a target directory.4. P1 fix — Protocol method signatures for TraceStore, EventStore, ArtifactStore. Phase 0 ANNEX D defined these Protocols with ... only — no method signatures. Without explicit method signatures, Grok Build cannot implement or test them. This chunk appends the full method signatures in the same amend-by-addition block as VectorStore. The signatures match the call sites already specified in CHUNK-1.1 (trace_store.write_event(session_id, node_type, failure_type, outcome, detail)) and CHUNK-1.6 (event_store.write_event(event_type, actor, artifact_id, ...), artifact_store.write(id, content, metadata), artifact_store.read(id)). This is the same pattern used for VectorStore: additive, not replacing any Phase 0 code.5. CHUNK-0.6 dependency. This chunk amends foundation/protocols.py, which CHUNK-0.6 tests via test_storage_contracts.py. Adding CHUNK-0.6 to DEPENDS-ON ensures the test update happens in order: CHUNK-1.0a adds the new Protocol methods, then CHUNK-1.0b implements store() as an upsert wrapper so the Phase 0 test still passes.The gate test verifies: (a) Chunk dataclass can be instantiated, (b) RetrievalResult dataclass works, (c) VectorStore Protocol has upsert, retrieve, delete methods, (d) TraceStore Protocol has write_event method, (e) EventStore Protocol has write_event method, (f) ArtifactStore Protocol has write and read methods, (g) existing Phase 0 schema enums are not broken.ANNEXfoundation/schemas.py (append to existing file):# --- Phase 1 additions (append only) ---from dataclasses import dataclass, field@dataclassclass Chunk:    """Unified retrieval chunk. Replaces RetrievalHit.    Used by VectorStore.retrieve() and retrieve_for_synthesis().    One type for the entire retrieval pipeline.    """    id: str    content: str | None = None    score: float = 0.0    metadata: dict = field(default_factory=dict)    domain: str | None = None@dataclassclass RetrievalResult:    """Result of retrieve_for_synthesis with low-confidence gate status."""    status: str  # "OK" | "INSUFFICIENT_MEMORY"    hits: list[Chunk]    max_confidence: float    message: str | None = Nonefoundation/protocols.py (amend VectorStore Protocol by addition):class VectorStore(Protocol):    """Vector store abstraction for pgvector / sqlite-vss swap.    Phase 1 amendment: added upsert, retrieve with query_vector, delete.    Phase 0 store() method is deprecated but retained for backward compat.    """    async def upsert(        self,        id: str,        embedding: list[float],        content: str,        metadata: dict,        domain: str | None = None,    ) -> None:        """Store or update a vector with metadata."""        ...    async def retrieve(        self,        query_vector: list[float],        domain: str | None = None,        top_k: int = 10,    ) -> list[Chunk]:        """Retrieve chunks by vector similarity."""        ...    async def delete(self, id: str) -> None:        """Delete a vector entry by id."""        ...    # Deprecated Phase 0 method — retained for backward compatibility    async def store(self, chunk: "Chunk") -> str:        """Deprecated: use upsert() instead. Returns chunk id."""        ...    async def count(self, domain: str | None = None) -> int:        """Count entries, optionally filtered by domain."""        ...class TraceStore(Protocol):    """Trace store for logging node execution events.    P1 fix: Phase 0 left this as `...` stub.    Method signature matches CHUNK-1.1 call site.    Method name is write_event (not log or append) — confirmed per R2'.    """    async def write_event(        self,        session_id: str,        node_type: str,        failure_type: str,        outcome: str,        detail: str | None = None,    ) -> None:        """Write a trace event for node execution."""        ...class EventStore(Protocol):    """Event store for recording ECS state transitions and lifecycle events.    P1 fix: Phase 0 left this as `...` stub.    Method signature matches CHUNK-1.6 call site.    """    async def write_event(        self,        event_type: str,        actor: str,        artifact_id: str,        from_state: str | None = None,        to_state: str | None = None,        **kwargs,    ) -> None:        """Write an event recording a state transition or lifecycle event."""        ...class ArtifactStore(Protocol):    """Artifact store for reading and writing generated content.    P1 fix: Phase 0 left this as `...` stub.    Method signatures match CHUNK-1.6 call sites.    """    async def write(self, id: str, content: str, metadata: dict) -> None:        """Write artifact content with metadata."""        ...    async def read(self, id: str) -> str:        """Read artifact content by id."""        ...tests/test_schema_additions.py:"""Verify Phase 1 schema additions do not break Phase 0."""import pytestfrom foundation.schemas import Chunk, ContractRule, EcsState, FailureType, RetrievalResultfrom foundation.protocols import ArtifactStore, EventStore, TraceStore, VectorStoredef test_chunk_dataclass():    c = Chunk(id="x", content="hello", score=0.9, metadata={"k": "v"}, domain="test")    assert c.id == "x"    assert c.score == 0.9def test_chunk_defaults():    c = Chunk(id="y")    assert c.content is None    assert c.metadata == {}    assert c.domain is Nonedef test_retrieval_result_dataclass():    r = RetrievalResult(status="OK", hits=[], max_confidence=0.0)    assert r.status == "OK"    assert r.message is Nonedef test_retrieval_result_insufficient():    r = RetrievalResult(status="INSUFFICIENT_MEMORY", hits=[], max_confidence=0.1, message="Low")    assert r.status == "INSUFFICIENT_MEMORY"def test_phase0_enums_still_work():    """Phase 0 enums must not be broken by Phase 1 additions."""    assert EcsState.GENERATED is not None    assert FailureType.C is not Nonedef test_vectorstore_protocol_has_required_methods():    """VectorStore must have upsert, retrieve, delete per Delta 1.    Uses hasattr for deterministic Protocol method detection.    dir() can pass even if methods are missing on Protocol classes.    """    assert hasattr(VectorStore, "upsert"), "VectorStore missing upsert method"    assert hasattr(VectorStore, "retrieve"), "VectorStore missing retrieve method"    assert hasattr(VectorStore, "delete"), "VectorStore missing delete method"    assert hasattr(VectorStore, "count"), "VectorStore missing count method"    assert hasattr(VectorStore, "store"), "VectorStore missing store method (Phase 0 compat)"def test_tracestore_protocol_has_write_event():    """P1: TraceStore must have write_event method matching CHUNK-1.1 call site."""    assert hasattr(TraceStore, "write_event"), "TraceStore missing write_event method"def test_eventstore_protocol_has_write_event():    """P1: EventStore must have write_event method matching CHUNK-1.6 call site."""    assert hasattr(EventStore, "write_event"), "EventStore missing write_event method"def test_artifactstore_protocol_has_write_and_read():    """P1: ArtifactStore must have write and read methods matching CHUNK-1.6 call sites."""    assert hasattr(ArtifactStore, "write"), "ArtifactStore missing write method"    assert hasattr(ArtifactStore, "read"), "ArtifactStore missing read method"CHUNK-1.0b: sqlite_vss VectorStore ImplementationCHUNK-1.0b: sqlite_vss VectorStore ImplementationPHASE: 1DEPENDS-ON: CHUNK-1.0aCODER-PROFILE: L2CONTEXT-BUDGET: ~5,000 tokensFILES:  adapter/vector/sqlite_vss_store.py  tests/test_sqlite_vss_store.pyINTERFACES:  class SqliteVssVectorStore(VectorStore):      def __init__(self, db_path: str, dimensions: int = 768) -> None: ...      async def upsert(self, id: str, embedding: list[float], content: str, metadata: dict, domain: str | None = None) -> None: ...      async def retrieve(self, query_vector: list[float], domain: str | None = None, top_k: int = 10) -> list[Chunk]: ...      async def delete(self, id: str) -> None: ...      async def count(self, domain: str | None = None) -> int: ...TESTS:  tests/test_sqlite_vss_store.pyGATE: uv run pytest tests/test_sqlite_vss_store.py tests/test_layering.py -xvsProseThis chunk implements the VectorStore protocol (as amended in CHUNK-1.0a) using sqlite_vss as the backend. Per the architecture (§2.2), sqlite_vss is the permitted fallback for Phase 0–2 alpha, and is the required vector backend for Phase 1 since pgvector is deferred to Phase 4.The adapter lives in the adapter/vector/ package, respecting the import boundary rules (§7.2): adapter may import foundation but not orchestration. The implementation must not import any network library (httpx, openai, anthropic).The retrieve method signature uses query_vector: list[float] (not str | list[float]). String queries are not accepted at the VectorStore level — the embedding step happens in the orchestration layer (CHUNK-1.1), which is the correct architectural boundary per §1.2 (the harness controls what the model sees). This resolves Delta 4: the VectorStore no longer raises NotImplementedError for string queries because it never accepts them. The function signature is clean: vector in, Chunks out.The return type is list[Chunk] (not list[RetrievalHit]), using the unified Chunk type defined in CHUNK-1.0a. This resolves Delta 1.The upsert method signature includes content: str and metadata: dict as explicit parameters (not buried inside metadata), matching the amended VectorStore Protocol.The sqlite_vss extension must be loaded at initialization. The database file is created if it does not exist. The virtual table uses the vss0 module with the specified dimensions. A companion vector_metadata table stores non-vector fields (content, domain, metadata JSON) since vss0 virtual tables only hold the vector column.ANNEXadapter/vector/sqlite_vss_store.py:"""sqlite_vss implementation of the VectorStore protocol.Phase 1 vector backend. pgvector adapter is deferred to Phase 4.Per §2.2: sqlite_vss is the permitted fallback for Phase 0-2 alpha."""from __future__ import annotationsimport jsonimport sqlite3from typing import Anyfrom foundation.protocols import VectorStorefrom foundation.schemas import Chunkclass SqliteVssVectorStore(VectorStore):    """VectorStore backed by sqlite_vss extension."""    def __init__(self, db_path: str, dimensions: int = 768) -> None:        self._db_path = db_path        self._dimensions = dimensions        self._conn = sqlite3.connect(db_path)        self._conn.enable_load_extension(True)        try:            self._conn.load_extension("vss0")        except sqlite3.OperationalError:            # F5 fix: second attempt tries common local path            self._conn.load_extension("./vss0.so")        self._conn.enable_load_extension(False)        self._init_tables()    def _init_tables(self) -> None:        cur = self._conn.cursor()        cur.execute("""            CREATE TABLE IF NOT EXISTS vector_metadata (                id TEXT PRIMARY KEY,                content TEXT,                domain TEXT,                metadata_json TEXT,                created_at TEXT DEFAULT (datetime('now'))            )        """)        cur.execute("""            CREATE INDEX IF NOT EXISTS idx_vm_domain            ON vector_metadata(domain)        """)        cur.execute(f"""            CREATE VIRTUAL TABLE IF NOT EXISTS vss_vectors            USING vss0(                embedding float[{self._dimensions}]            )        """)        self._conn.commit()    async def upsert(        self,        id: str,        embedding: list[float],        content: str,        metadata: dict[str, Any] | None = None,        domain: str | None = None,    ) -> None:        cur = self._conn.cursor()        # Delete existing entry if present (upsert semantics)        cur.execute("SELECT rowid FROM vector_metadata WHERE id = ?", (id,))        existing = cur.fetchone()        if existing:            cur.execute("DELETE FROM vss_vectors WHERE rowid = ?", (existing[0],))            cur.execute("DELETE FROM vector_metadata WHERE id = ?", (id,))        meta_json = json.dumps(metadata or {})        cur.execute(            "INSERT INTO vector_metadata (id, content, domain, metadata_json) VALUES (?, ?, ?, ?)",            (id, content, domain, meta_json),        )        rowid = cur.lastrowid        emb_str = json.dumps(embedding)        cur.execute("INSERT INTO vss_vectors(rowid, embedding) VALUES (?, ?)", (rowid, emb_str))        self._conn.commit()    async def retrieve(        self,        query_vector: list[float],        domain: str | None = None,        top_k: int = 10,    ) -> list[Chunk]:        emb_str = json.dumps(query_vector)        domain_filter = ""        params_domain: list[Any] = []        if domain is not None:            domain_filter = "AND m.domain = ?"            params_domain = [domain]        sql = f"""            SELECT m.id, m.content, v.distance, m.domain, m.metadata_json            FROM vss_vectors v            JOIN vector_metadata m ON v.rowid = m.rowid            WHERE vss_vectors MATCH ?            AND v.k = ?            {domain_filter}            ORDER BY v.distance        """        cur = self._conn.cursor()        cur.execute(sql, [emb_str, top_k] + params_domain)        results = []        for row in cur.fetchall():            id_, content, distance, domain_val, meta_json = row            score = max(0.0, 1.0 - distance) if distance is not None else 0.0            results.append(Chunk(                id=id_,                content=content,                score=score,                metadata=json.loads(meta_json) if meta_json else {},                domain=domain_val,            ))        return results    async def delete(self, id: str) -> None:        cur = self._conn.cursor()        cur.execute("SELECT rowid FROM vector_metadata WHERE id = ?", (id,))        row = cur.fetchone()        if row:            cur.execute("DELETE FROM vss_vectors WHERE rowid = ?", (row[0],))            cur.execute("DELETE FROM vector_metadata WHERE id = ?", (id,))            self._conn.commit()    async def count(self, domain: str | None = None) -> int:        cur = self._conn.cursor()        if domain:            cur.execute("SELECT COUNT(*) FROM vector_metadata WHERE domain = ?", (domain,))        else:            cur.execute("SELECT COUNT(*) FROM vector_metadata")        return cur.fetchone()[0]    # Deprecated Phase 0 method — retained for backward compat    # F1 fix: implemented as upsert wrapper with zero-vector    # so Phase 0 test_storage_contracts.py continues to pass.    async def store(self, chunk: Chunk) -> str:        """Phase 0 compat: store with zero-vector embedding.        Real usage should call upsert() with an actual embedding.        This wrapper exists so test_storage_contracts.py passes.        """        await self.upsert(            chunk.id,            [0.0] * self._dimensions,            chunk.content or "",            chunk.metadata,            chunk.domain,        )        return chunk.id    def close(self) -> None:        self._conn.close()tests/test_sqlite_vss_store.py:"""Tests for SqliteVssVectorStore.Requires sqlite_vss extension to be available.All tests run locally with no network calls."""import mathimport pytesttry:    import sqlite3    conn = sqlite3.connect(":memory:")    conn.enable_load_extension(True)    conn.load_extension("vss0")    conn.close()    HAS_VSS = Trueexcept Exception:    HAS_VSS = Falsepytestmark = pytest.mark.skipif(not HAS_VSS, reason="sqlite_vss not available")from adapter.vector.sqlite_vss_store import SqliteVssVectorStorefrom foundation.schemas import Chunk@pytest.fixturedef store(tmp_path):    db_path = str(tmp_path / "test_vectors.db")    s = SqliteVssVectorStore(db_path=db_path, dimensions=4)    yield s    s.close()def _unit(values: list[float]) -> list[float]:    norm = math.sqrt(sum(v * v for v in values))    return [v / norm for v in values] if norm > 0 else values@pytest.mark.asyncioasync def test_upsert_and_retrieve(store):    emb1 = _unit([1.0, 0.0, 0.0, 0.0])    emb2 = _unit([0.0, 1.0, 0.0, 0.0])    await store.upsert("doc1", emb1, content="Document one", domain="test", metadata={"k": "v"})    await store.upsert("doc2", emb2, content="Document two", domain="test")    results = await store.retrieve(emb1, domain="test", top_k=2)    assert len(results) >= 1    assert isinstance(results[0], Chunk)    assert results[0].id == "doc1"    assert results[0].content == "Document one"    assert results[0].score > 0.5@pytest.mark.asyncioasync def test_domain_filter(store):    emb = _unit([1.0, 0.0, 0.0, 0.0])    await store.upsert("doc_a", emb, content="Alpha", domain="alpha")    await store.upsert("doc_b", emb, content="Beta", domain="beta")    results_alpha = await store.retrieve(emb, domain="alpha", top_k=10)    assert all(r.domain == "alpha" for r in results_alpha)@pytest.mark.asyncioasync def test_upsert_overwrite(store):    emb = _unit([1.0, 0.0, 0.0, 0.0])    await store.upsert("doc1", emb, content="Version 1")    await store.upsert("doc1", emb, content="Version 2")    assert await store.count() == 1@pytest.mark.asyncioasync def test_delete(store):    emb = _unit([1.0, 0.0, 0.0, 0.0])    await store.upsert("doc1", emb, content="To delete")    await store.delete("doc1")    assert await store.count() == 0@pytest.mark.asyncioasync def test_count_by_domain(store):    emb = _unit([1.0, 0.0, 0.0, 0.0])    await store.upsert("d1", emb, content="X", domain="x")    await store.upsert("d2", emb, content="Y", domain="x")    await store.upsert("d3", emb, content="Z", domain="y")    assert await store.count(domain="x") == 2    assert await store.count(domain="y") == 1    assert await store.count() == 3@pytest.mark.asyncioasync def test_retrieve_returns_chunk_type(store):    """Delta 1: VectorStore.retrieve must return list[Chunk], not list[RetrievalHit]."""    emb = _unit([1.0, 0.0, 0.0, 0.0])    await store.upsert("d1", emb, content="Test", domain="test")    results = await store.retrieve(emb, domain="test")    for r in results:        assert isinstance(r, Chunk)CHUNK-1.1: retrieve_for_synthesis (Config-Loaded)CHUNK-1.1: retrieve_for_synthesisPHASE: 1DEPENDS-ON: CHUNK-1.0b, CHUNK-0.2, CHUNK-0.4CODER-PROFILE: L2CONTEXT-BUDGET: ~6,000 tokensFILES:  orchestration/retrieval.py  tests/test_retrieve_for_synthesis.pyINTERFACES:  async def retrieve_for_synthesis(query: str, domain: str, vector_store: VectorStore, embed_fn: Callable[[str], list[float]], trace_store: TraceStore, config: AipConfig | dict | None = None) -> RetrievalResult: ...  def rerank(hits: list[Chunk], domain: str, weights: RerankWeights) -> list[Chunk]: ...  def fake_embed(text: str) -> list[float]: ...  @dataclass  class RerankWeights:      semantic: float      recency: float      authority: float      frequency: floatTESTS:  tests/test_retrieve_for_synthesis.pyGATE: uv run pytest tests/test_retrieve_for_synthesis.py tests/test_layering.py -xvsProseThis chunk implements the L2 retrieval function specified in §8.2 and the four-factor reranking specified in §8.3, with two critical fixes from the meta-review:Delta 4 fix — Embedding pipeline gap: In Phase 1, there is no real embedding slot. The retrieve_for_synthesis function accepts an embed_fn: Callable[[str], list[float]] parameter, which converts the text query to a vector before calling VectorStore.retrieve(query_vector=...). In CI, this is the deterministic fake_embed() function. In production (Phase 3+), this will be replaced by the embedding slot call. The function signature makes the embedding step explicit rather than raising NotImplementedError. This aligns with §1.2 (the harness controls retrieval) and §1.3 (the harness mediates everything the model sees).Delta 5 fix — Config-loaded weights: Per §1.8, harness components must be toggleable. The RerankWeights dataclass is populated from config/aip.config.toml under the [retrieval] section, not hardcoded. The config loader from CHUNK-0.2 provides the values. Defaults exist as fallbacks (matching §8.3 defaults) but the gate test must verify that the function reads from config, not from hardcoded constants. The confidence_threshold is also config-loaded from [retrieval].confidence_threshold with default 0.30.R2 fix — TraceStore logging: The function accepts a trace_store: TraceStore parameter. On INSUFFICIENT_MEMORY results, it writes a trace event with node_type="L2", failure_type="A" (Missing Context per Appendix E — the retrieval did not find adequate context memory), and outcome="insufficient_memory". F2 fix: changed from failure_type="B" (Procedural Gap) to "A" (Missing Context) per Appendix E taxonomy. Low confidence indicates missing context, not a procedural gap. This ensures Sexton classifies retrieval failures correctly.Defaults match §8.3 but are overridden by config — satisfies §1.8 toggleable requirement. The RerankWeights.from_config() method uses .get() with §8.3 defaults as fallback values when the config section is absent, which is good engineering practice. The gate test verifies that explicit config values override these defaults. The config section in aip.config.toml is:[retrieval]confidence_threshold = 0.30weight_semantic = 0.60weight_recency = 0.15weight_authority = 0.15weight_frequency = 0.10ANNEXorchestration/retrieval.py:"""L2 retrieval: low-confidence gate + four-factor reranking.Implements §8.2 (retrieve_for_synthesis) and §8.3 (reranking).Delta 5: weights loaded from config, not hardcoded.Delta 4: embed_fn parameter for text-to-vector conversion.R2: TraceStore logging on INSUFFICIENT_MEMORY."""from __future__ import annotationsimport hashlibimport mathfrom dataclasses import dataclassfrom datetime import datetime, timezonefrom typing import Callablefrom foundation.protocols import TraceStore, VectorStorefrom foundation.schemas import Chunk, RetrievalResult@dataclassclass RerankWeights:    """Four-factor reranking weights per §8.3.    All values come from config/aip.config.toml [retrieval].    Defaults match §8.3 but are overridden by config —    satisfies §1.8 toggleable requirement.    """    semantic: float    recency: float    authority: float    frequency: float    @classmethod    def from_config(cls, config: dict) -> "RerankWeights":        """Load from [retrieval] config section."""        r = config.get("retrieval", {})        return cls(            semantic=r.get("weight_semantic", 0.60),            recency=r.get("weight_recency", 0.15),            authority=r.get("weight_authority", 0.15),            frequency=r.get("weight_frequency", 0.10),        )# Authority mapping per §8.3_AUTHORITY_SCORES = {    "approved": 1.0,    "reviewed": 0.75,    "provisional": 0.5,    "raw": 0.25,}_RECENCY_HALF_LIFE_DAYS = 30.0def _compute_recency(metadata: dict) -> float:    created_at = metadata.get("created_at")    if not created_at:        return 1.0    try:        dt = datetime.fromisoformat(created_at)        now = datetime.now(timezone.utc)        age_days = (now - dt).total_seconds() / 86400        return math.exp(-0.693 * age_days / _RECENCY_HALF_LIFE_DAYS)    except (ValueError, TypeError):        return 1.0def _compute_authority(metadata: dict) -> float:    authority = metadata.get("authority", "raw")    return _AUTHORITY_SCORES.get(authority, 0.25)def _compute_frequency(metadata: dict) -> float:    count = metadata.get("access_count", 0)    return min(1.0, count / 10.0) if count > 0 else 0.5def rerank(hits: list[Chunk], domain: str, weights: RerankWeights) -> list[Chunk]:    """Apply four-factor reranking per §8.3."""    ranked = []    for hit in hits:        semantic = hit.score        recency = _compute_recency(hit.metadata)        authority = _compute_authority(hit.metadata)        frequency = _compute_frequency(hit.metadata)        final_score = (            weights.semantic * semantic            + weights.recency * recency            + weights.authority * authority            + weights.frequency * frequency        )        ranked.append(Chunk(            id=hit.id,            content=hit.content,            score=final_score,            metadata=hit.metadata,            domain=hit.domain,        ))    ranked.sort(key=lambda h: h.score, reverse=True)    return rankeddef fake_embed(text: str, dimensions: int = 768) -> list[float]:    """Deterministic fake embedding for Phase 1 CI testing.    Produces a unit-normalized vector derived from text hash.    Real embedding slot wired in Phase 3.    NOT for production use.    """    h = hashlib.sha256(text.encode()).digest()    # Expand hash to fill dimensions    vec = []    for i in range(dimensions):        byte_idx = i % len(h)        vec.append(h[byte_idx] / 255.0)    # Normalize to unit vector    norm = math.sqrt(sum(v * v for v in vec))    return [v / norm for v in vec] if norm > 0 else vecasync def retrieve_for_synthesis(    query: str,    domain: str,    vector_store: VectorStore,    embed_fn: Callable[[str], list[float]],    trace_store: TraceStore,    config: "AipConfig | dict | None" = None,    top_k: int = 10,) -> RetrievalResult:    """L2 retrieval with low-confidence rejection gate per §8.2.    Delta 4: embed_fn converts text to vector before VectorStore call.    Delta 5: confidence_threshold and weights from config.    R2: logs INSUFFICIENT_MEMORY to trace_store.    Args:        query: Text query (embedded via embed_fn before VectorStore call).        domain: Domain for retrieval filtering.        vector_store: VectorStore protocol instance.        embed_fn: Text-to-vector function (fake_embed in CI, embedding slot in Phase 3).        trace_store: TraceStore for logging retrieval failures.        config: AipConfig (Pydantic) from config loader, or dict for tests.                 F3 fix: accepts both AipConfig and dict with model_dump fallback.        top_k: Number of results to retrieve.    Returns:        RetrievalResult with OK or INSUFFICIENT_MEMORY status.    """    # F3 fix: accept AipConfig (Pydantic) or dict    cfg = config.model_dump() if hasattr(config, "model_dump") else (config or {})    retrieval_cfg = cfg.get("retrieval", {})    confidence_threshold = retrieval_cfg.get("confidence_threshold", 0.30)    weights = RerankWeights.from_config(cfg)    # Embed the text query into a vector    query_vector = embed_fn(query)    # Retrieve from VectorStore    results = await vector_store.retrieve(        query_vector=query_vector, domain=domain, top_k=top_k    )    if not results:        # R2: Log to trace_store — F2: failure_type="A" (Missing Context), not "B"        await trace_store.write_event(            session_id="retrieval",            node_type="L2",            failure_type="A",            outcome="insufficient_memory",            detail=f"No results for domain={domain}",        )        return RetrievalResult(            status="INSUFFICIENT_MEMORY",            hits=[],            max_confidence=0.0,            message="Insufficient Library grounding. Specify source material or reduce scope.",        )    max_conf = max(r.score for r in results)    if max_conf < confidence_threshold:        # F2: failure_type="A" (Missing Context), not "B" (Procedural Gap)        await trace_store.write_event(            session_id="retrieval",            node_type="L2",            failure_type="A",            outcome="insufficient_memory",            detail=f"Max confidence {max_conf:.2f} below threshold {confidence_threshold}",        )        return RetrievalResult(            status="INSUFFICIENT_MEMORY",            hits=results,            max_confidence=max_conf,            message="Insufficient Library grounding. Specify source material or reduce scope.",        )    reranked = rerank(results, domain, weights)    return RetrievalResult(        status="OK",        hits=reranked,        max_confidence=reranked[0].score if reranked else 0.0,    )tests/test_retrieve_for_synthesis.py:"""Tests for retrieve_for_synthesis and four-factor reranking.Delta 4: Uses fake_embed for text-to-vector conversion.Delta 5: Asserts config-sourced confidence_threshold and weights."""from __future__ import annotationsfrom typing import Anyimport pytestfrom foundation.schemas import Chunk, RetrievalResultfrom orchestration.retrieval import (    RerankWeights,    fake_embed,    rerank,    retrieve_for_synthesis,)# --- Fake stores for testing ---class FakeVectorStore:    def __init__(self, hits: list[Chunk] | None = None):        self._hits = hits or []    async def retrieve(self, query_vector, domain=None, top_k=10):        return self._hits    async def upsert(self, id, embedding, content="", metadata=None, domain=None):        pass    async def delete(self, id):        pass    async def count(self, domain=None):        return len(self._hits)class FakeTraceStore:    def __init__(self):        self.events: list[dict] = []    async def write_event(self, session_id, node_type, failure_type=None,                          outcome=None, detail=None, **kw):        self.events.append({            "session_id": session_id,            "node_type": node_type,            "failure_type": failure_type,            "outcome": outcome,            "detail": detail,        })def _chunk(id: str, score: float, authority: str = "raw",           created_at: str | None = None, access_count: int = 0) -> Chunk:    return Chunk(        id=id,        content=f"Content for {id}",        score=score,        metadata={            "authority": authority,            "created_at": created_at,            "access_count": access_count,        },        domain="test",    )def _default_config() -> dict:    return {        "retrieval": {            "confidence_threshold": 0.30,            "weight_semantic": 0.60,            "weight_recency": 0.15,            "weight_authority": 0.15,            "weight_frequency": 0.10,        }    }# --- Delta 4: fake_embed tests ---def test_fake_embed_returns_correct_dimensions():    vec = fake_embed("hello world", dimensions=768)    assert len(vec) == 768def test_fake_embed_is_deterministic():    assert fake_embed("test") == fake_embed("test")def test_fake_embed_is_unit_vector():    import math    vec = fake_embed("test")    norm = math.sqrt(sum(v * v for v in vec))    assert abs(norm - 1.0) < 0.01# --- Retrieval tests ---@pytest.mark.asyncioasync def test_insufficient_memory_empty_results():    store = FakeVectorStore(hits=[])    trace = FakeTraceStore()    result = await retrieve_for_synthesis(        "query", "test", store, fake_embed, trace, _default_config()    )    assert result.status == "INSUFFICIENT_MEMORY"    assert result.max_confidence == 0.0    # R2: trace event logged    assert len(trace.events) == 1    assert trace.events[0]["failure_type"] == "A"  # F2: Missing Context, not Procedural Gap@pytest.mark.asyncioasync def test_insufficient_memory_below_threshold():    low_hits = [_chunk("doc1", 0.10), _chunk("doc2", 0.20)]    store = FakeVectorStore(hits=low_hits)    trace = FakeTraceStore()    result = await retrieve_for_synthesis(        "query", "test", store, fake_embed, trace, _default_config()    )    assert result.status == "INSUFFICIENT_MEMORY"    assert len(trace.events) == 1@pytest.mark.asyncioasync def test_ok_above_threshold():    hits = [_chunk("doc1", 0.80), _chunk("doc2", 0.50)]    store = FakeVectorStore(hits=hits)    trace = FakeTraceStore()    result = await retrieve_for_synthesis(        "query", "test", store, fake_embed, trace, _default_config()    )    assert result.status == "OK"    assert len(result.hits) == 2    assert result.hits[0].score >= result.hits[1].score    assert len(trace.events) == 0  # No trace event on success# --- Delta 5: config-loaded values ---def test_confidence_threshold_from_config():    """Delta 5: threshold must come from config, not hardcoded."""    config = _default_config()    assert config["retrieval"]["confidence_threshold"] == 0.30def test_rerank_weights_from_config():    """Delta 5: weights must come from config."""    w = RerankWeights.from_config(_default_config())    assert w.semantic == 0.60    assert w.recency == 0.15def test_rerank_weights_config_overrides_defaults():    config = {        "retrieval": {            "weight_semantic": 1.0,            "weight_recency": 0.0,            "weight_authority": 0.0,            "weight_frequency": 0.0,        }    }    w = RerankWeights.from_config(config)    assert w.semantic == 1.0# --- Rerank tests ---def test_rerank_preserves_order_for_equal_metadata():    hits = [_chunk("a", 0.9), _chunk("b", 0.7), _chunk("c", 0.5)]    w = RerankWeights.from_config(_default_config())    reranked = rerank(hits, "test", w)    assert [h.id for h in reranked] == ["a", "b", "c"]def test_rerank_authority_boosts_approved():    hits = [        _chunk("raw", 0.80, authority="raw"),        _chunk("approved", 0.70, authority="approved"),    ]    w = RerankWeights.from_config(_default_config())    reranked = rerank(hits, "test", w)    assert reranked[0].id == "approved" or reranked[0].score > reranked[1].score# --- F3: AipConfig / dict dual acceptance ---def test_config_accepts_dict():    """F3: retrieve_for_synthesis must accept plain dict config."""    store = FakeVectorStore(hits=[_chunk("d1", 0.80)])    trace = FakeTraceStore()    # Should not raise    import asyncio    result = asyncio.get_event_loop().run_until_complete(        retrieve_for_synthesis("query", "test", store, fake_embed, trace, _default_config())    )    assert result.status == "OK"def test_config_model_dump_fallback():    """F3: If config has model_dump (Pydantic AipConfig), it should be called."""    class FakeAipConfig:        def model_dump(self) -> dict:            return _default_config()    store = FakeVectorStore(hits=[_chunk("d1", 0.80)])    trace = FakeTraceStore()    import asyncio    result = asyncio.get_event_loop().run_until_complete(        retrieve_for_synthesis("query", "test", store, fake_embed, trace, FakeAipConfig())    )    assert result.status == "OK"CHUNK-1.2: structural_validateCHUNK-1.2: structural_validatePHASE: 1DEPENDS-ON: CHUNK-0.3, CHUNK-0.4CODER-PROFILE: L1CONTEXT-BUDGET: ~4,000 tokensFILES:  foundation/validation.py  tests/test_structural_validate.pyINTERFACES:  def structural_validate(output: str, rules: list[ValidationRule] | None = None) -> ValidationResult: ...  @dataclass  class ValidationRule:      rule_id: str      check: Callable[[str], bool]      failure_type: str  # "C" | "E"      message: str      model_gen_assumption: str | None  @dataclass  class ValidationResult:      passed: bool      failure_type: str | None  # "C" | "E" | None      failure_detail: str | None      checks_run: int      checks_failed: list[str]TESTS:  tests/test_structural_validate.pyGATE: uv run pytest tests/test_structural_validate.py -xvsProseUnchanged from Rev 1.0. This chunk implements L3a Stage 1 deterministic structural validation per §9.1. It consumes zero tokens — no model call is involved. The function is pure: it takes input and returns a result with no side effects.R2 note — TraceStore logging: The caller of structural_validate (the workflow node) is responsible for writing trace events on validation failure. The validation function itself does not accept a TraceStore parameter because it is a pure foundation-layer function with no side effects. The orchestration layer logs the trace event using the ValidationResult.failure_type and failure_detail fields. This preserves the import boundary: foundation/validation.py does not import TraceStore.Default rules for Phase 1 (unchanged, all tagged with model_gen_assumption="deepseek-v3-0324 or qwen3-4b" per §1.8):min_length — output must be at least 100 characters. Failure type C.no_false_success_patterns — completion claim without 200+ chars after it. Failure type E.required_section_markers — at least one section header pattern. Failure type C.ANNEXfoundation/validation.py — unchanged from Rev 1.0. See original spec. tests/test_structural_validate.py — unchanged from Rev 1.0. See original spec.CHUNK-1.3: Synthesis Node StubCHUNK-1.3: Synthesis Node StubPHASE: 1DEPENDS-ON: CHUNK-1.1, CHUNK-0.2CODER-PROFILE: L1CONTEXT-BUDGET: ~4,000 tokensFILES:  orchestration/nodes/synthesis.py  tests/test_synthesis_node.pyINTERFACES:  async def synthesize(query: str, domain: str, retrieval_result: RetrievalResult, model_slot: str = "synthesis") -> SynthesisOutput: ...  @dataclass  class SynthesisOutput:      content: str      model_slot: str      model_name: str      token_count_in: int      token_count_out: int      latency_ms: intTESTS:  tests/test_synthesis_node.pyGATE: uv run pytest tests/test_synthesis_node.py tests/test_layering.py -xvsProseUnchanged from Rev 1.0, except:DEPENDS-ON corrected from CHUNK-0.6 to CHUNK-0.2 (config system, including slot resolution function).Model name resolution uses config/aip.config.toml via the config loader from CHUNK-0.2, not a hardcoded <slot>-slot-model placeholder. In stub mode, the slot is resolved through the config but no API call is made.The stub’s output content must pass the structural_validate checks from CHUNK-1.2.ANNEXorchestration/nodes/synthesis.py — substantially unchanged from Rev 1.0 except model name resolution reads from config: def _resolve_model_name(slot: str, config: dict | None = None) -> str:    """Resolve model slot to model name via aip.config.toml.    In Phase 1 stub mode, returns the configured model name    without making any API calls or network requests.    """    cfg = config or {}    models = cfg.get("models", {})    slot_cfg = models.get(slot, {})    return slot_cfg.get("model", f"<{slot}-slot-unconfigured>")tests/test_synthesis_node.py — unchanged from Rev 1.0. CHUNK-1.4: Adversarial Eval StubCHUNK-1.4: Adversarial Eval StubPHASE: 1DEPENDS-ON: CHUNK-1.2, CHUNK-1.3CODER-PROFILE: L1CONTEXT-BUDGET: ~4,000 tokensFILES:  orchestration/nodes/adversarial_eval.py  tests/test_adversarial_eval.pyINTERFACES:  async def adversarial_eval(synthesis_output: SynthesisOutput, validation_result: ValidationResult, eval_criteria: list[EvalCriterion] | None = None) -> EvalResult: ...  @dataclass  class EvalCriterion:      criterion_id: str      name: str      description: str      model_gen_assumption: str | None  @dataclass  class EvalResult:      passed: bool      scores: dict[str, float]      requires_deep_eval: bool      critique: str | None = NoneTESTS:  tests/test_adversarial_eval.pyGATE: uv run pytest tests/test_adversarial_eval.py tests/test_layering.py -xvsProseUnchanged from Rev 1.0. The four criteria from §F.3 are retained with model_gen_assumption="deepseek-v3-0324 or qwen3-4b" tags. Stub mode returns deterministic passing scores.ANNEXorchestration/nodes/adversarial_eval.py — unchanged from Rev 1.0. tests/test_adversarial_eval.py — unchanged from Rev 1.0. CHUNK-1.5: DEFINER Gate StubCHUNK-1.5: DEFINER Gate StubPHASE: 1DEPENDS-ON: CHUNK-1.3, CHUNK-1.4CODER-PROFILE: L1CONTEXT-BUDGET: ~4,000 tokensFILES:  orchestration/nodes/definer_gate.py  tests/test_definer_gate.pyINTERFACES:  async def definer_gate(synthesis_output: SynthesisOutput, validation_result: ValidationResult, eval_result: EvalResult, mode: DefinerGateMode = DefinerGateMode.AUTO_APPROVE_STUB) -> DefinerDecision: ...  @dataclass  class DefinerDecision:      action: str  # "approve" | "reject" | "revise"      reason: str | None = None      approved_by: str | None = None  class DefinerGateMode(Enum):      AUTO_APPROVE_STUB = "auto_approve_stub"TESTS:  tests/test_definer_gate.pyGATE: uv run pytest tests/test_definer_gate.py tests/test_layering.py -xvsProseUnchanged from Rev 1.0. Per §1.7, no artifact may bypass DEFINER gates. Auto-approve only when both validation and evaluation pass.ANNEXorchestration/nodes/definer_gate.py — unchanged from Rev 1.0. tests/test_definer_gate.py — unchanged from Rev 1.0. CHUNK-1.6: Commit StubCHUNK-1.6: Commit StubPHASE: 1DEPENDS-ON: CHUNK-1.5, CHUNK-0.4, CHUNK-0.5CODER-PROFILE: L1CONTEXT-BUDGET: ~4,000 tokensFILES:  orchestration/nodes/commit.py  tests/test_commit_node.pyINTERFACES:  async def commit_artifact(synthesis_output: SynthesisOutput, definer_decision: DefinerDecision, project_id: str, work_unit_id: str, artifact_store: ArtifactStore, ecs_store: EcsStore, event_store: EventStore) -> ArtifactRef: ...  @dataclass  class ArtifactRef:      artifact_id: str      ecs_state: str  # "GENERATED" per §9.3      project_id: str      work_unit_id: str      committed_at: strTESTS:  tests/test_commit_node.pyGATE: uv run pytest tests/test_commit_node.py tests/test_layering.py -xvsProseUnchanged from Rev 1.0 except for R3 fix — ECS transition recorded in event_log and P2 fix — ecs_store.transition with actor/reason:The commit_artifact function now accepts an event_store: EventStore parameter. After the ECS state transition from SPECIFIED to GENERATED, it writes an event to the event store recording the transition with actor="definer_gate", action="ecs_transition", from_state="SPECIFIED", to_state="GENERATED", and artifact_id. This ensures the event log has a full audit trail of the DEFINER-approved state change, which Sexton can later use to track artifact lifecycle.P2 fix: The ecs_store.transition() call now passes actor="definer_gate" and reason="DEFINER approved" per the Phase 0 contract signature (artifact_id, from_state, to_state, actor, reason, superseded_by=None) and §1.7 sovereignty requirement. The FakeEcsStore in the test is updated to accept and record these parameters. This matches both the Phase 0 EcsStore Protocol and the architecture’s requirement that every state transition has an identifiable actor and reason.The test must assert that: (a) the event store received the transition event, (b) the event has actor="definer_gate", (c) the event records both from_state and to_state, and (d) the ecs_store.transition call includes actor and reason.ANNEXorchestration/nodes/commit.py:"""Commit node for Workflow 0.1.Writes approved artifact to ArtifactStore, transitions ECS state,and records the transition in EventStore.Per §1.7: only proceeds on DEFINER approval.Per §9.3: initial ECS state is GENERATED.R3: ECS transition recorded in event_log with actor='definer_gate'."""from __future__ import annotationsimport hashlibfrom dataclasses import dataclassfrom datetime import datetime, timezonefrom foundation.protocols import ArtifactStore, EcsStore, EventStorefrom orchestration.nodes.definer_gate import DefinerDecisionfrom orchestration.nodes.synthesis import SynthesisOutputclass CommitBlockedError(Exception):    """Raised when commit is attempted without DEFINER approval."""@dataclassclass ArtifactRef:    """Reference to a committed artifact."""    artifact_id: str    ecs_state: str  # "GENERATED" per §9.3    project_id: str    work_unit_id: str    committed_at: strdef _compute_artifact_id(content: str, project_id: str, work_unit_id: str) -> str:    """Deterministic artifact ID for provenance per §1.5."""    raw = f"{project_id}:{work_unit_id}:{content}"    return hashlib.sha256(raw.encode()).hexdigest()[:16]async def commit_artifact(    synthesis_output: SynthesisOutput,    definer_decision: DefinerDecision,    project_id: str,    work_unit_id: str,    artifact_store: ArtifactStore,    ecs_store: EcsStore,    event_store: EventStore,) -> ArtifactRef:    """Commit approved artifact to store.    Per §1.7: only commits if DEFINER approved.    Per §9.3: ECS state transitions to GENERATED.    R3: transition recorded in event_log with actor='definer_gate'.    """    if definer_decision.action != "approve":        raise CommitBlockedError(            f"Cannot commit artifact: DEFINER decision was '{definer_decision.action}'. "            f"Reason: {definer_decision.reason}"        )    artifact_id = _compute_artifact_id(        synthesis_output.content, project_id, work_unit_id    )    now = datetime.now(timezone.utc).isoformat()    # Write artifact content    await artifact_store.write(        id=artifact_id,        content=synthesis_output.content,        metadata={            "model_slot": synthesis_output.model_slot,            "model_name": synthesis_output.model_name,            "token_count_in": synthesis_output.token_count_in,            "token_count_out": synthesis_output.token_count_out,            "project_id": project_id,            "work_unit_id": work_unit_id,        },    )    # Transition ECS state: SPECIFIED -> GENERATED (P2: includes actor/reason per Phase 0 contract)    await ecs_store.transition(        artifact_id=artifact_id,        from_state="SPECIFIED",        to_state="GENERATED",        actor="definer_gate",        reason="DEFINER approved",    )    # R3: Record transition in event_log    await event_store.write_event(        event_type="ecs_transition",        actor="definer_gate",        artifact_id=artifact_id,        from_state="SPECIFIED",        to_state="GENERATED",        project_id=project_id,        work_unit_id=work_unit_id,        timestamp=now,    )    return ArtifactRef(        artifact_id=artifact_id,        ecs_state="GENERATED",        project_id=project_id,        work_unit_id=work_unit_id,        committed_at=now,    )tests/test_commit_node.py:"""Tests for commit node.R3: includes event_log assertion for ECS transition."""from __future__ import annotationsfrom typing import Anyimport pytestfrom foundation.protocols import ArtifactStore, EcsStore, EventStorefrom orchestration.nodes.commit import ArtifactRef, CommitBlockedError, commit_artifactfrom orchestration.nodes.definer_gate import DefinerDecisionfrom orchestration.nodes.synthesis import SynthesisOutputclass FakeArtifactStore:    def __init__(self):        self._store: dict[str, dict] = {}    async def write(self, id: str, content: str, metadata: dict | None = None, **kw: Any) -> None:        self._store[id] = {"content": content, "metadata": metadata}    async def read(self, id: str) -> dict | None:        return self._store.get(id)class FakeEcsStore:    def __init__(self):        self._states: dict[str, str] = {}        self._transitions: list[dict] = []    async def transition(self, artifact_id: str, from_state: str, to_state: str,                         actor: str | None = None, reason: str | None = None,                         superseded_by: str | None = None) -> None:        current = self._states.get(artifact_id)        if current and current != from_state:            raise ValueError(f"Expected state {from_state}, got {current}")        self._states[artifact_id] = to_state        self._transitions.append({            "artifact_id": artifact_id,            "from_state": from_state,            "to_state": to_state,            "actor": actor,            "reason": reason,        })    async def get_state(self, artifact_id: str) -> str | None:        return self._states.get(artifact_id)class FakeEventStore:    def __init__(self):        self.events: list[dict] = []    async def write_event(self, **kwargs: Any) -> None:        self.events.append(kwargs)def _synthesis() -> SynthesisOutput:    return SynthesisOutput(        content="# Result\n\n" + "x" * 200,        model_slot="synthesis",        model_name="<synthesis-slot-model>",        token_count_in=10,        token_count_out=50,        latency_ms=100,    )def _approved() -> DefinerDecision:    return DefinerDecision(action="approve", approved_by="stub:auto_approve")def _rejected() -> DefinerDecision:    return DefinerDecision(action="reject", reason="Failed eval")@pytest.mark.asyncioasync def test_commit_on_approval():    artifact_store = FakeArtifactStore()    ecs_store = FakeEcsStore()    event_store = FakeEventStore()    result = await commit_artifact(        _synthesis(), _approved(), "proj1", "wu1",        artifact_store, ecs_store, event_store,    )    assert isinstance(result, ArtifactRef)    assert result.ecs_state == "GENERATED"    assert result.project_id == "proj1"@pytest.mark.asyncioasync def test_commit_writes_to_artifact_store():    artifact_store = FakeArtifactStore()    ecs_store = FakeEcsStore()    event_store = FakeEventStore()    result = await commit_artifact(        _synthesis(), _approved(), "proj1", "wu1",        artifact_store, ecs_store, event_store,    )    stored = await artifact_store.read(result.artifact_id)    assert stored is not None@pytest.mark.asyncioasync def test_commit_transitions_ecs_state():    artifact_store = FakeArtifactStore()    ecs_store = FakeEcsStore()    event_store = FakeEventStore()    result = await commit_artifact(        _synthesis(), _approved(), "proj1", "wu1",        artifact_store, ecs_store, event_store,    )    state = await ecs_store.get_state(result.artifact_id)    assert state == "GENERATED"@pytest.mark.asyncioasync def test_r3_ecs_transition_recorded_in_event_log():    """R3: ECS transition must be recorded in event_log with actor='definer_gate'."""    artifact_store = FakeArtifactStore()    ecs_store = FakeEcsStore()    event_store = FakeEventStore()    result = await commit_artifact(        _synthesis(), _approved(), "proj1", "wu1",        artifact_store, ecs_store, event_store,    )    assert len(event_store.events) == 1    event = event_store.events[0]    assert event["event_type"] == "ecs_transition"    assert event["actor"] == "definer_gate"    assert event["from_state"] == "SPECIFIED"    assert event["to_state"] == "GENERATED"    assert event["artifact_id"] == result.artifact_id@pytest.mark.asyncioasync def test_p2_ecs_transition_includes_actor_and_reason():    """P2: ecs_store.transition must be called with actor='definer_gate' and reason='DEFINER approved'."""    artifact_store = FakeArtifactStore()    ecs_store = FakeEcsStore()    event_store = FakeEventStore()    await commit_artifact(        _synthesis(), _approved(), "proj1", "wu1",        artifact_store, ecs_store, event_store,    )    assert len(ecs_store._transitions) == 1    t = ecs_store._transitions[0]    assert t["actor"] == "definer_gate"    assert t["reason"] == "DEFINER approved"    assert t["from_state"] == "SPECIFIED"    assert t["to_state"] == "GENERATED"@pytest.mark.asyncioasync def test_commit_blocked_on_reject():    artifact_store = FakeArtifactStore()    ecs_store = FakeEcsStore()    event_store = FakeEventStore()    with pytest.raises(CommitBlockedError, match="DEFINER decision was 'reject'"):        await commit_artifact(            _synthesis(), _rejected(), "proj1", "wu1",            artifact_store, ecs_store, event_store,        )    # No event logged on blocked commit    assert len(event_store.events) == 0@pytest.mark.asyncioasync def test_deterministic_artifact_id():    """Same content + project + work_unit = same artifact ID (§1.5 provenance)."""    stores1 = (FakeArtifactStore(), FakeEcsStore(), FakeEventStore())    stores2 = (FakeArtifactStore(), FakeEcsStore(), FakeEventStore())    r1 = await commit_artifact(_synthesis(), _approved(), "proj1", "wu1", *stores1)    r2 = await commit_artifact(_synthesis(), _approved(), "proj1", "wu1", *stores2)    assert r1.artifact_id == r2.artifact_idCHUNK-1.7: Network Isolation and Model-Name GateCHUNK-1.7: Network Isolation and Model-Name GatePHASE: 1DEPENDS-ON: CHUNK-1.0a, CHUNK-1.0b, CHUNK-1.1, CHUNK-1.2, CHUNK-1.3, CHUNK-1.4, CHUNK-1.5, CHUNK-1.6CODER-PROFILE: L1CONTEXT-BUDGET: ~4,000 tokensFILES:  tests/test_no_network.py  tests/test_no_hardcoded_models.pyINTERFACES:  (test infrastructure only — no production code)TESTS:  tests/test_no_network.py  tests/test_no_hardcoded_models.pyGATE: uv run pytest tests/test_no_network.py tests/test_no_hardcoded_models.py -xvsProseUnchanged from Rev 1.0. These two cross-cutting gates enforce determinism: no network calls and no hardcoded model names in production code.test_no_network.py scans foundation/, orchestration/, adapter/ for imports of httpx, openai, anthropic. test_no_hardcoded_models.py scans the same directories for literal model name strings, excluding config/, tests/, and *.toml files (since config/aip.config.toml is the authoritative place for model name mappings per §4.1).ANNEXtests/test_no_network.py — unchanged from Rev 1.0. tests/test_no_hardcoded_models.py — unchanged from Rev 1.0. Phase 1 Green Gate SummaryAll of the following test files must pass before Phase 2 can begin. Each gate is runnable in CI with no secrets, no network calls, and no model API keys.Test FileValidatesCHUNKtests/test_schema_additions.pyChunk, RetrievalResult dataclasses; VectorStore Protocol amendment; Phase 0 enums intact1.0atests/test_sqlite_vss_store.pyVectorStore adapter: upsert, retrieve, delete, domain filter; returns list[Chunk]1.0btests/test_retrieve_for_synthesis.pyL2 retrieval + low-confidence gate + four-factor reranking + fake_embed + config-loaded values + TraceStore logging + AipConfig/dict dual acceptance1.1tests/test_structural_validate.pyL3a Stage 1 deterministic validation, §1.8 assumption tagging1.2tests/test_synthesis_node.pySynthesis stub returns valid output, passes validation1.3tests/test_adversarial_eval.pyL3b stub scoring, §1.8 assumption tagging1.4tests/test_definer_gate.pyDEFINER sovereignty: approve/reject/revise logic1.5tests/test_commit_node.pyArtifact commit + ECS transition + event_log record + commit blocking1.6tests/test_no_network.pyNo httpx/openai/anthropic imports in production code1.7tests/test_no_hardcoded_models.pyNo hardcoded model name strings in production code1.7tests/test_layering.pyImport boundaries: foundation isolated, orchestration depends on foundation only0.6/1.xtests/test_trace_schema.pytrace_events schema, failure_type enum, outcome enum (from Phase 0)0.7Phase 1 aggregate gate command:uv run pytest \  tests/test_schema_additions.py \  tests/test_sqlite_vss_store.py \  tests/test_retrieve_for_synthesis.py \  tests/test_structural_validate.py \  tests/test_synthesis_node.py \  tests/test_adversarial_eval.py \  tests/test_definer_gate.py \  tests/test_commit_node.py \  tests/test_no_network.py \  tests/test_no_hardcoded_models.py \  tests/test_layering.py \  tests/test_trace_schema.py \  -xvsAll 12 test files must pass. Zero failures. Zero skips (except sqlite_vss if extension unavailable).Cross-Cutting Compliance ChecklistRequirementVerification MethodDelta§1.8 model_gen_assumption taggingEvery ValidationRule and EvalCriterion carries non-null model_gen_assumptionVerified in tests§1.8 toggleable harnessRerankWeights + confidence_threshold loaded from config/aip.config.tomlD5 fix§4.1 no hardcoded model namestest_no_hardcoded_models.py grep gate; config is the only exceptionVerified§7.2 import boundariestest_layering.pyVerifiedNetwork isolationtest_no_network.pyVerified§9.3 ECS state transitions + event_logtest_commit_node.py verifies SPECIFIED → GENERATED + event_log entry with actor=‘definer_gate’R3 fix§9.1 L3a zero tokensstructural_validate is pure Python, no model callVerified§8.2 confidence threshold from configtest_retrieve_for_synthesis.py asserts config-sourced thresholdD5 fix§8.3 reranking weights from configRerankWeights.from_config() + testD5 fixUnified Chunk type (no RetrievalHit)test_schema_additions.py + test_sqlite_vss_store.pyD1 fixVectorStore Protocol alignmenttest_schema_additions.py verifies upsert/retrieve/delete on ProtocolD1 fixPhase 0 file integritytest_schema_additions.py verifies Phase 0 enums still workD3 fixfake_embed for CItest_retrieve_for_synthesis.py validates fake_embedD4 fixTraceStore logging on INSUFFICIENT_MEMORYtest_retrieve_for_synthesis.py verifies trace eventR2 fixDeterministic CIAll gates runnable with no secrets, no network, no API keysVerifiedsqlite_vss for vector storeCHUNK-1.0b adapterVerifiedpgvector deferred to Phase 4Not present in any Phase 1 codeVerifiedConfig file name consistencyconfig/aip.config.toml everywhere (not models.toml)D2 fixValidation Checklist (Pre-Build)Run this mental gate before feeding any chunk to Grok Build:☒ All Protocol method names match between Phase 0 ANNEX D amendment and Phase 1 CHUNK interfaces (D1)☒ No Phase 1 chunk modifies a Phase 0 file unless listed in FILES with “(append only)” (D3)☒ Every model_gen_assumption field is non-null in test fixtures (verified by test_all_default_rules_tagged_with_assumption)☒ Config file name is consistent: config/aip.config.toml everywhere (D2)☒ All 12 Phase 1 tests can run with uv run pytest -k "not network" on a laptop with no sqlite_vss (they skip cleanly)☒ CHUNK-1.1 gate asserts confidence_threshold comes from config, not hardcoded 0.30 (D5)☒ CHUNK-1.6 gate asserts ECS transition recorded in event_log with actor=‘definer_gate’ (R3)☒ Every ANNEX code block has ESTIMATED_TOKENS annotation (R4)☒ VectorStore.store() implemented as upsert wrapper with zero-vector, not NotImplementedError (F1)☒ retrieve_for_synthesis logs failure_type=“A” (Missing Context), not “B” (Procedural Gap) (F2)☒ CHUNK-1.1 config parameter accepts AipConfig or dict with model_dump fallback (F3)☒ CHUNK-1.1 prose acknowledges defaults match §8.3, overridden by config (F4)☒ sqlite_vss load_extension second attempt tries “./vss0.so” (F5)☒ test_schema_additions uses hasattr instead of dir() for Protocol method detection (R1’)☒ CHUNK-1.0a DEPENDS-ON includes CHUNK-0.6 (R3’)☒ CHUNK-1.1 test file has ESTIMATED_TOKENS annotation (R4’)☒ TraceStore.write_event, EventStore.write_event, ArtifactStore.write/read method signatures appended to protocols.py (P1)☒ ecs_store.transition call includes actor=‘definer_gate’ and reason=‘DEFINER approved’ (P2)☒ schemas.py append shows from dataclasses import dataclass, field — no NameError (P3)